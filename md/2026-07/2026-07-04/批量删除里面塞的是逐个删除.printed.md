一、映射改造 fork 完成 —— 52 passed,但有个硬障碍

机制搭好了(全绿 52 passed / 245 assertions,改动留在 worktree 等你 review diff):

- 新建 OperationLogRegistry(register/models/resolve,target 支持字段名或闭包)
- Observer 守卫换成 Registry::resolve()(未登记→null→跳过,防递归保留)
- record() 改 Registry 展开,仍支持传 subject,所以 role_assigned 那种手动调用不用改
- 删了 ProvidesOperationContext 接口 + AdminUser 的契约实现
- Provider 注册 + 批量 observe;新增 OperationLogRegistryTest

但"接单条"只成功接了 3 个,不是你要的全部:

```
┌─────────────────────────────────┬───────────┬──────────────────────────────────────────────────────────────────────────────────┐
│ 模块 │ 接了单条? │ 原因 │
├─────────────────────────────────┼───────────┼──────────────────────────────────────────────────────────────────────────────────┤
│ AdminUser / Role / MembersLevel │ ✅ │ 批量走 query-builder、单条走实例,不双记 │
├─────────────────────────────────┼───────────┼──────────────────────────────────────────────────────────────────────────────────┤
│ MembersList(会员本体) │ ❌ │ batchDelete 逐个 $member->delete() → 挂 observer 就和 logBatch 双记 │
├─────────────────────────────────┼───────────┼──────────────────────────────────────────────────────────────────────────────────┤
│ CustomerGroup/Tag/Level │ ❌ │ 删除走实例 $row->delete(),单条又已被 count==1 的 record 记过 → 双记(实测冒 2 条) │
└─────────────────────────────────┴───────────┴──────────────────────────────────────────────────────────────────────────────────┘
```

fork 按我拦它的提醒,正确跳过了会双记的模块,还写了测试锁定"不双记"。这是 RecyclableTrait(软删要求实例删)和 observer 的天然冲突,不是 bug。

▎ module 值是推断的:Role='setting'、MembersLevel='members'(我觉得 Role 该是 access/role 更贴,你 review diff 时按需改)。application 是确定的。
